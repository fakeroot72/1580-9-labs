1. Ввод данных
    * Проверка на правильную длинну
    * Отделение часов и минут
    * Проверка что часы и минуты это числа
    * Проверка что часы и минуты находятся в пределах

2. Определение времени
    * Подсчёт "полного" времени в минутах
    @ Если п. в. = 0
        * вернуть "полночь"
    @ иначе если принадлежит (0; 6 * 60]
        * вернуть часы в правильном падеже + минуты в правильном падеже + "ночи"
    @ иначе если принадлежит (6 * 60; 12 * 60)
        * тоже самое что и с (0; 6 * 60] но + "утра" а не + "ночи"
    @ иначе если п. в. = 12 * 60
        * вернуть полдень
    @ иначе если принадлежит (12 * 60; 18 * 60)
        * вернуть (часы если == 12 иначе часы - 12) в правильном падеже + минуты в правильном падеже + "дня"
    @ иначе если принадлежит [18 * 60; 24 * 60)
        * вернуть (часы - 12) в правильном падеже + минуты в правильном падеже + "ночи"
    @ иначе
        # что-то пошло не так
        * вернуть пустую строку
    @ если минуты = 0
        * поменять минуты на "ровно"

3. Вывод данных
    вывести то что вернула основная часть программыo
